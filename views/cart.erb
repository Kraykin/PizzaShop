<h2>Your cart</h2>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Title</th>
			<th>Quantity</th>
		</tr>
	</thead>
	<tbody>
		<% @orders.each do |row| %>
			<tr>
	  			<td><%= row[0] %></td>
	  			<td><%= row[1] %></td>
	  		</tr>
	  	<% end %>
	</tbody>
</table>